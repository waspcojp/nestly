「<%= @space.nest.title %>」に新しいスペース「<%= @space.title %>」が作られました。
--------------------
<%= @space.description %>
--------------------
<% if ( @notice ) %>
このスペースからの通知を受けたい場合は、以下のURLをクリックして下さい。
<%=  notice_watch_url(@notice.watch.accept_token, on_off: "on") %>

<% if ( @space.preparable?(@notice.user) ) %>
このメールのアドレスにメールを送ることにより、新しいエントリを作ることが出来ます。
<% end %>
<% end %>
<% if ( @invite ) %>
あなたは、まだ「<%= @invite.nest.title %>」への参加表明をしていません。

<%= l @invite.expired_at.in_time_zone('Tokyo'), format: :short %>までに以下のURLをクリックして参加の意思表示をして下さい。

<%= invite_url(@invite.invitation_token) %>

参加しない場合、このメールに心あたりのない場合も、上のURLよりその旨申告することが出来ます。
<% end %>
